# 📖 Enterprise AI QA Orchestration: Execution User Guide

Welcome to the **Enterprise AI QA Orchestration** system. This guide provides step-by-step instructions on how to execute the automated testing workflow, monitor real-time progress via the AI Dashboard, and analyze execution artifacts.

---

## 🛠️ Prerequisites

Before running the workflow, ensure you have the following installed on your system:
- **Node.js** (v18 or higher)
- **npm** (comes with Node.js)
- **Google Chrome** (for headed browser execution)
- **Git** (optional, for version control)

---

## 🚀 Setup Instructions

1. **Install Root Dependencies**:
   Open your terminal in the project root and run:
   ```bash
   npm install
   ```

2. **Install Playwright Browsers**:
   Ensure the browser binaries are available:
   ```bash
   npx playwright install chromium
   ```

3. **Dashboard Setup**:
   Navigate to the dashboard application and install its specific dependencies:
   ```bash
   cd dashboard/dashboard-app
   npm install
   ```

---

## 🔄 Running the Orchestration Workflow

The system is designed to be orchestrated via a real-time dashboard. Follow these steps in order:

### Step 1: Start the Dashboard Backend
The backend handles WebSocket communication between the orchestrator and the frontend.
```bash
# From the root directory
node dashboard/backend/server.js
```
*The server will start on `http://localhost:5000`.*

### Step 2: Start the Dashboard Frontend
Open a new terminal and launch the React dashboard:
```bash
cd dashboard/dashboard-app
npm run dev
```
*Open your browser to the URL provided (usually `http://localhost:5173`).*

### Step 3: Trigger the Orchestrator
In a third terminal, run the orchestrator for a specific Jira ticket (e.g., `SCRUM-101`):
```bash
node scripts/orchestrate.js SCRUM-101
```

**What happens during orchestration?**
1. **Requirement Fetching**: Analyzes requirements from `artifacts/{JIRA_ID}/requirements.json`.
2. **Execution**: Launches a headed Chromium browser via Playwright.
3. **Real-time Updates**: Sends logs and pass/fail metrics to the Dashboard.
4. **Self-Healing**: If a locator fails, the AI attempts to "heal" the script.
5. **Reporting**: Generates a detailed HTML report and AI insights.

---

## 🧪 Alternative: Standalone Execution Script

If you want to run a quick verification of the OrangeHRM module without the full dashboard overhead, you can use the standalone script:

```bash
node automated_execution.js
```
**This script will:**
- Create 10 unique employees in OrangeHRM.
- Verify them via the Search module.
- Generate `ExecutionData.xlsx` (Excel) and `ExecutionReport.html`.
- Create a release note and an email preview.

---

## 📊 Understanding Execution Artifacts

After a workflow finishes, you can find the results in the following locations:

| Artifact | Location | Description |
| :--- | :--- | :--- |
| **HTML Report** | `artifacts/{JIRA_ID}/report.html` | Visual summary of test results. |
| **Test Results** | `artifacts/{JIRA_ID}/results.json` | Raw JSON data of every test step. |
| **AI Insights** | `artifacts/{JIRA_ID}/insights.json` | Root cause analysis and flaky detection. |
| **Excel Data** | `./ExecutionData.xlsx` | Tabulated employee and API test case data. |
| **Release Note** | `./release_notes/ReleaseNote.md` | Summary of changes and execution status. |
| **Bug Evidence** | `artifacts/bug_screenshot.png` | Screenshot of the failure (if any). |

---

## 🛠️ Troubleshooting

- **Connection Error**: Ensure the backend `server.js` is running before starting the orchestrator.
- **Playwright Timeouts**: If the application is slow, increase the `timeout` in `playwright.config.ts`.
- **Headed Mode**: To run tests silently, modify `scripts/orchestrate.js` to change `--headed` to `--headless` in the `execSync` command.

---

*Generated by Antigravity AI Orchestrator.*
